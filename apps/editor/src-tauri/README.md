# Tauri shell

The Tauri 2 shell will be introduced with the editor application work. This directory exists now to reserve the native boundary without adding an unconfigured native runtime or pretending the editor is implemented.
