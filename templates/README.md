# Project templates

`first-story/` is the first working creator project template. It uses the ordinary `project.toml` loader, two independently compiled story files, the core runtime, and the web-capable player path.

Kinetic Story, Conversation Mystery, Text RPG Expedition, and the generated Tauri game shell remain planned. A template is listed here only after its sources load and compile through the same public packages used by creator projects.
